### [Install bootstrap](https://create-react-app.dev/docs/adding-bootstrap/)

1. `npm install bootstrap`
2. Add CSS to the beggining of `index.js`:

```
import 'bootstrap/dist/css/bootstrap.css';
```

